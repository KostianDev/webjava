version: '3.8'
services:
  wiremock:
    image: wiremock/wiremock:3.5.4
    ports:
      - "8089:8080"
    volumes:
      - ./wiremock:/home/wiremock
    command: ["--verbose", "--global-response-templating", "--port", "8080", "--root-dir", "/home/wiremock"]
